# 开发日志 — 2026-05-16

## 今日完成

- [x] 解决 Gradle 下载：腾讯云镜像 `mirrors.cloud.tencent.com/gradle`
- [x] 解决 Maven 下载：阿里云镜像 `maven.aliyun.com`
- [x] 解决 Flutter 引擎下载：`FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn`
- [x] 解决 Dart 包下载：`PUB_HOSTED_URL=https://pub.flutter-io.cn`
- [x] 解决中文路径编码：`subst X: C:\Users\黄金秋` 映射虚拟盘
- [x] 解决 Java 25 不兼容：下载 JDK 21（清华镜像，27 秒完成）
- [x] 解决 desugaring：启用 coreLibraryDesugaring
- [x] **Android APK 构建成功** — `app-debug.apk` (142MB)

## 构建配置总结

### 环境变量（每次构建前设置）
```bash
export JAVA_HOME="c:/Users/黄金秋/jdk-21/jdk-21.0.11+10"
export ANDROID_HOME="X:/Android"
export FLUTTER_STORAGE_BASE_URL="https://storage.flutter-io.cn"
export PUB_HOSTED_URL="https://pub.flutter-io.cn"
```

### 文件修改记录
- `android/gradle/wrapper/gradle-wrapper.properties` → 腾讯云 Gradle 镜像
- `android/settings.gradle.kts` → 阿里云 Maven 镜像
- `android/build.gradle.kts` → 阿里云 Maven 镜像
- `android/app/build.gradle.kts` → 启用 desugaring
- `android/local.properties` → X: 盘路径

## 待做

- [ ] 启动模拟器运行测试
- [ ] `flutter run` 热重载开发
- [ ] iOS 构建（需 macOS）
- [ ] Release 签名打包

## 备注

- Gradle 9.x 尚不存在，只能 JDK 21 + Gradle 8.14 组合
- `subst X:` 映射需每次重启后重新执行，可加启动脚本
- Tsinghua 镜像对 JDK 下载极快（7MB/s），对 Flutter 工具下载也快
